{
  "name": "xdc-vault-topup",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network apothem"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@openzeppelin/contracts": "^4.0.0",
    "dotenv": "^16.5.0",
    "ethers": "^5.0.0",
    "hardhat": "^2.12.0"
  }
}
